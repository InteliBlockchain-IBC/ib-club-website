# Ignorar pastas específicas
/.cache/
/node_modules/

.vercel

# Fotos originais em alta resolução (8,4 MB). Ficam fora do histórico:
# são fonte, não entregável, e a origem delas é o Canva do clube.
# As derivadas web usadas na landing estão em design-concepts/assets/fotos/.
/ai/

# Ponto de retorno de antes de a pasta existir no git. O git é a rede agora.
design-concepts/*.bak.html
